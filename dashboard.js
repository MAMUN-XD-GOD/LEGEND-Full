async function fetchSignals(){try{let r=await fetch('/signals.log');if(!r.ok){document.getElementById('status').textContent='No signals';return}let t=await r.text();let lines=t.trim().split('\n').filter(Boolean).slice(-30).reverse();let ul=document.getElementById('signals');ul.innerHTML='';for(const l of lines){try{const o=JSON.parse(l);const li=document.createElement('li');li.textContent=`${o.pair} ${o.direction} conf=${o.confidence}`;ul.appendChild(li);}catch(e){}}document.getElementById('status').textContent='Online'}catch(e){document.getElementById('status').textContent='Offline'}}setInterval(fetchSignals,2000);fetchSignals();